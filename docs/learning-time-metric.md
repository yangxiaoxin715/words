# 学习用时统计口径

## 口径

学习用时按闪卡事件累加。

每张卡从出现到用户点击反馈，或自动判为新朋友，记为一次 `elapsed_ms`。为了避免用户中途离开导致时间虚高，单张卡最多按 10 秒计入有效用时。

## 当前展示

- 首页展示 `今日用时`。
- 数据详情展示 `核心用时` 和 `导入用时`。
- 本轮结束页展示 `本轮用时`。

## 规则

- 核心词和导入词包都会计入今日用时。
- 本轮用时只统计当前这次进入刷词页后的有效用时。
- 后端保存原始 `elapsed_ms`，看板汇总时按 10 秒封顶计算。
